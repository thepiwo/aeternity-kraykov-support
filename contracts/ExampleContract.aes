contract Token =
  public function amount : () => int

contract ExampleContract =
  type state = ()

  public function test(token_option : option(Token)) : int =
    switch(token_option)
      Some(token) => token.amount()
      None => 0
